"""classical_poetry vertical — package marker.

The second real literary vertical: it CONSUMES the shared literary contracts
(task_envelope, review_contract, artifact_manifest, source_registry/provenance)
exactly as fiction_writing does, but its craft state — prosody (平仄/韵部/粘对/
孤平/三平尾/诗体) — is vertical-PRIVATE and never lifted into the shared layer.
"""
from __future__ import annotations
